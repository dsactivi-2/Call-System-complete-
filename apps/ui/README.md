# UI Placeholder

This directory is reserved for a future web frontend (e.g. Next.js, SvelteKit, or a simple admin dashboard).

To scaffold a new UI here:

```bash
cd apps/ui
npx create-next-app@latest .
```

For now, leave this directory empty or add a static HTML placeholder.
